# GitHub (existing)
GITHUB_TOKEN=

# football-data.org — free token for live World Cup scores (optional).
# Register at https://www.football-data.org/client/register
# Without it, /world-cup falls back to the openfootball backbone (no in-play scores).
FOOTBALL_DATA_TOKEN=
